# Optional: `gh auth login` is used when neither GitHub variable is set.
GITHUB_TOKEN=
# Required by the default hosted text-embedding-3-large provider.
OPENAI_API_KEY=
# Public build-time metadata only; defaults to https://haroldmartin.me.
# Use an HTTP(S) origin without a path, query, or fragment.
VITE_SITE_URL=https://haroldmartin.me
